export enum DEVICE_SIZE {
    DESKTOP = 1440,
    LAPTOP = 786,
    MOBILE = 320,
}

export enum DEVICE {
    DESKTOP = 'desktop',
    LAPTOP = 'laptop',
    MOBILE = 'mobile',
}

export enum THEME_MODE {
    DARK_MODE = 'dark-mode',
    LIGHT_MODE = 'light-mode'
}
