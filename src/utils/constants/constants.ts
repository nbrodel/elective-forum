export enum DEVICE_SIZE {
    DESKTOP = 1440,
    LAPTOP = 768,
    MOBILE = 320,
}

export enum DEVICE {
    DESKTOP = 'desktop',
    LAPTOP = 'laptop',
    MOBILE = 'mobile',
}

export enum ROUTES {
    HOME = '/',
    ELECTIVES = '/electives',
    PROFILE = '/profile',
    LOGOUT = '/logout'
}

export const RegExp = {
    username: /^[A-Za-z]+$/
}
